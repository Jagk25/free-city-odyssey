# SPEC.md — Free City: Odyssey

The product contract lives in `docs/FREE_CITY_KIMI_K3_BUILD_PLAN.md` §3
(feature inventory: world, characters, interiors, mini-games, story,
progression, UX). That section is the single source of truth.

Edit rule (per AGENTS.md): only the ORCHESTRATOR may change the spec, and
only after a DECISIONS.log entry.

Reference build: the v9.1 single-file prototype remains the gameplay
reference for VISION-QA comparisons. It is archived documentation, not the
production codebase.
